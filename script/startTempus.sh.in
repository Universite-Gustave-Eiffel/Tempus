#!/bin/sh
BIN_DIR=${CMAKE_INSTALL_PREFIX}/bin
PLUGIN_DIR=${PLUGIN_INSTALL_DIR}
TEMPUS_DATA_DIRECTORY=${CMAKE_INSTALL_PREFIX}/${TEMPUS_INSTALL_DATA_DIRECTORY}/data $BIN_DIR/wps -c $PLUGIN_DIR -p 9000 $@

